import { connect } from 'react-redux';

import {

} from '../modules/partner'
import Partner from '../components/'

const mapStateToProps = (state) => ({

})

const mapActionCreators = {

};

export default connect(mapStateToProps, mapActionCreators)(Partner);

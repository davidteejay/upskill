import { connect } from 'react-redux';

import {

} from '../modules/explore'
import Explore from '../components/'

const mapStateToProps = (state) => ({

})

const mapActionCreators = {

};

export default connect(mapStateToProps, mapActionCreators)(Explore);
